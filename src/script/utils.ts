function somma(num1: number, num2: number): string {
    return `la somma di ${num1} e ${num2} è ${num1 + num2}`;
}

function moltiplicazione(num1: number, num2: number): string {
    return `il prodotto di ${num1} e ${num2} è ${num1 * num2}`;
}

export { somma, moltiplicazione };